pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Build steps
                echo 'Building the application...'
            }
        }

        stage('Test') {
            steps {
                // Test steps
                echo 'Running tests...'
            }
        }

        stage('Deploy') {
            steps {
                // Deploy steps
                echo 'Deploying the application...'
            }
        }
    }
}
